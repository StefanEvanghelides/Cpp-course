#include <iostream>
#include <string>

namespace Icmbuild
{
    extern char version[];
    extern char years[];
    extern char author[];
};

using namespace std;

int sum(int *values, size_t numEl); //sum 1
double sum(double *values, size_t numEl); //sum 2

